# Phase 16 — Application Development & Deployment

## Purpose
Streamlit application that resolves seven compact analytical artifacts and presents forecasts, model evidence and inventory scenarios without retraining.

## Starting State
Phase 15 PASSED; all analytical artifacts regenerated by the pipeline re-run.

## Inputs
`data/analysis/*.csv` (pipeline output) and the committed `deploy/artifacts/` bundle (7 CSVs).

## Commands Executed
- CMD-P16-01: launched headless on the documented port via a detached `Start-Process` helper: `.venv\Scripts\python.exe -m streamlit run app/streamlit_app.py --server.port 8523 --server.headless true`. Startup stdout confirms "Local URL: http://localhost:8523"; stderr shows Uvicorn listening. Evidence: `command-001-stdout.txt`, `command-001-stderr.txt`, `app.pid`, helper `start_app.ps1`.
- CMD-P16-02: `curl` homepage — **HTTP 200** (`command-002-http-check.txt`; page captured in `homepage.html`, 7,459 B, `<title>Streamlit</title>`). Health endpoint `/_stcore/health` — **HTTP 200**.
- CMD-P16-03: `pytest tests/test_application.py -q -p no:cacheprovider` — exit 0, **36 passed**, 5.88 s. Evidence: `command-003.txt`.
- Shutdown: both app processes stopped; port 8523 released (only TIME_WAIT teardown sockets remained).

## Tests
36/36 PASSED (matches documented 36). Includes import-safety, secret-scan, artifact resolution and a headless HTTP startup test, plus SHA-256 reconciliation of the `deploy/artifacts/` bundle against pipeline output.

## Artifacts
No new repository artifacts (application reads existing files). `deploy/artifacts/` unchanged (tracked bundle intact).

## Expected Results
Per docs: app starts headless, answers HTTP 200, resolves artifacts env → `data/analysis/` → `deploy/artifacts/`, never retrains; hosted public deployment not claimed.

## Actual Results
Exactly as documented for local deployment. Observed listener PID 21852 on 0.0.0.0:8523 and [::]:8523; HTTP 200 homepage and health. Hosted Streamlit deployment remains unexecuted (needs one-time owner authorisation) — consistent with the documented limitation.

## Discrepancies
None. (One audit-tooling mishap: an initial launch attempt failed on shell-quoting of `$p.Id`; the PS1 helper resolved it. Recorded in `command-log.md`.)

## Changes
None.

## Regression Checks
Test suite reconciles the deployment bundle against the freshly regenerated pipeline output by SHA-256 — no drift after the full re-run.

## Final Status
PASSED
